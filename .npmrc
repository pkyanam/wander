# Keep workspace package versions strict and predictable.
link-workspace-packages=true
prefer-workspace-packages=true
# Required so Next.js standalone tracing can resolve hoisted peer deps cleanly.
public-hoist-pattern[]=*next*
public-hoist-pattern[]=*@clerk*
