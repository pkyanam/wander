import { InterestPicker } from "@/components/onboarding/interest-picker";

// Onboarding is optional and anonymous — interests are saved to localStorage.
export default function OnboardingPage() {
  return <InterestPicker />;
}
